import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color.fromRGBO(152, 135, 255, 1); //Usada para cor principal do aplicativo
  static const primary2 = Color.fromRGBO(91, 81, 153, 1); //Usada como cor secundária do aplicativo
  static const verdePadrao = Color.fromRGBO(74, 207, 12, 1);
  static const vermelhoPadrao = Color(0xFFE83F5B); // Usada para serviços coplmentares, como flush bar, alertas, etc...
  static const backbackgroundHiddenDrawer = Colors.deepPurple;
  static const background = Color(0xFFFFFFFF);
  static const cardColor = Color.fromRGBO(217, 217, 217, 1);
  static const corBotao = Color.fromRGBO(40, 204, 184, 1);
}
