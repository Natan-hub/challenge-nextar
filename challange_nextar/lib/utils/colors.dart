import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color.fromRGBO(255, 51, 76, 1); //Usada para cor principal do aplicativo
  static const primary2 = Color.fromRGBO(204,40,60, 1); //Usada como cor secundária do aplicativo
  static const vermelhoPadrao = Color(0xFFE83F5B); // Usada para serviços coplmentares, como flush bar, alertas, etc...
  static const background = Color(0xFFFFFFFF);
  static const cardColor = Color.fromRGBO(217, 217, 217, 1);
}
